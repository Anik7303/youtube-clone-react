import React from "react";

// css stylesheets
import "./SideMenu.scss";

const SideMenu = (props) => {
    return (
        <React.Fragment>
            <h1>SideMenu</h1>
        </React.Fragment>
    );
};

export default SideMenu;
